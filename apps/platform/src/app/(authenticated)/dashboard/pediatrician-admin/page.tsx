import { PageContainer } from "@/components/page-container";
import { Card, CardContent, CardHeader, CardTitle } from "@clearpath/ui/components/card";

export default function PediatricianAdminDashboardPage() {
  return (
    <PageContainer title="Pediatrician Dashboard" description="Referral status and diagnostic reports">
      <Card>
        <CardHeader>
          <CardTitle>Pediatrician Dashboard</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">This section is under construction.</p>
        </CardContent>
      </Card>
    </PageContainer>
  );
}
