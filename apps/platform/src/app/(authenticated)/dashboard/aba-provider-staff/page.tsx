import { PageContainer } from "@/components/page-container";
import { Card, CardContent, CardHeader, CardTitle } from "@clearpath/ui/components/card";

export default function AbaProviderStaffDashboardPage() {
  return (
    <PageContainer title="ABA Provider Dashboard" description="Referral tracking and case status">
      <Card>
        <CardHeader>
          <CardTitle>ABA Provider Dashboard</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">This section is under construction.</p>
        </CardContent>
      </Card>
    </PageContainer>
  );
}
